# دليل تشغيل Malg — خطوة بخطوة (من الصفر)

اقرأ الملف ده بالترتيب، من غير ما تقفز خطوة. كل خطوة مبنية على اللي قبلها.

---

## الجزء 1: إيه اللي عندك دلوقتي في الفولدر

```
malg/
├── public/                ← ده اللي المستخدم بيشوفه (الموقع)
│   ├── index.html         ← الشاشات التلاتة: تحميل / دخول / التطبيق
│   ├── style.css          ← الشكل والألوان (دارك، وفيه تصميم للتلفون وتصميم للكمبيوتر)
│   ├── js/
│   │   ├── phone.js       ← منطق أرقام الموبايل (نفس الملف بيشتغل في المتصفح وفي السيرفر)
│   │   ├── format.js      ← التواريخ وتجميع الرسايل ومعاينة آخر رسالة
│   │   ├── paths.js       ← بناء مسارات Firebase و id المحادثة
│   │   ├── store.js       ← تخزين محلي (IndexedDB) على جهاز المستخدم — تاريخ الشات يفضل عنده
│   │   └── app.js         ← اللي بيكلم السيرفر + الرسايل الفورية (Firebase)
│   ├── icons/             ← أيقونات التطبيق (بتتولّد — شوف الجزء 9)
│   ├── firebase-client-config.js ← بيانات مشروع Firebase (تملأها بنفسك — شوف الجزء 8)
│   ├── manifest.json      ← ملف بيخلي الموقع "قابل للتحويل لتطبيق"
│   └── sw.js              ← Service Worker: كاش للملفات، ومفيش كاش للرسايل
├── api/
│   ├── auth/
│   │   ├── signup.js      ← السيرفر: إنشاء حساب
│   │   ├── login.js       ← السيرفر: تسجيل دخول
│   │   └── me.js          ← السيرفر: التحقق من الجلسة
│   ├── users/
│   │   └── search.js      ← السيرفر: البحث برقم كامل بالظبط (مفيش بحث جزئي)
│   └── firebase-token.js  ← السيرفر: بيصدر توكن Firebase بعد التحقق من الجلسة
├── lib/
│   ├── db.js              ← الاتصال بقاعدة البيانات
│   ├── auth.js            ← أدوات التشفير والتوكن
│   ├── session.js         ← التحقق من هوية المستخدم من التوكن
│   ├── phone.js           ← بيستخدم نفس ملف الأرقام بتاع الواجهة (مفيش نسختين)
│   └── firebase.js        ← تهيئة Firebase Admin SDK
├── tests/                 ← 94 اختبار (شوف الجزء 10)
├── tools/make-icons.py    ← مولّد الأيقونات
├── schema.sql             ← أوامر إنشاء الجداول في قاعدة البيانات
├── firebase-database-rules.json ← قواعد أمان الرسايل (تلصقها في Firebase Console — الجزء 8)
├── package.json           ← قائمة المكتبات وأوامر التشغيل
├── vercel.json            ← إعدادات النشر
├── .env.example           ← نموذج متغيرات البيئة (انسخه لـ .env.local للتشغيل المحلي)
├── _old/                  ← نسخ قديمة محفوظة، مش جزء من التطبيق (تقدر تسيبها)
└── .gitignore             ← ملفات منمنعش نرفعها على GitHub
```

> **مهم:** الكود القديم كان مكتوب في ملف واحد `public/script.js`، ونسخ من
> نفس الملفات كانت متكررة في جذر المشروع. كل النسخ القديمة اتنقلت لـ `_old/`
> **من غير مسح**. المتصفح بيقرا من `public/` بس، فلو رجّعت أي ملف من `_old/`
> لبراها ممكن يتعارض مع الجديد.


---

## الجزء 2: قاعدة البيانات (Neon) — إنشاء الجداول

عندك مشروع Neon اسمه `sweet-tooth-22771875` فعلاً (شفته في رسالتك). الخطوات:

1. روح على https://console.neon.tech وسجل دخول.
2. افتح المشروع `sweet-tooth-22771875`.
3. من القائمة الجانبية، دوس على **SQL Editor**.
4. افتح ملف `schema.sql` اللي عندك في الفولدر، انسخ **كل المحتوى** بتاعه.
5. الصقه في الـ SQL Editor في Neon.
6. دوس **Run** (أو Ctrl+Enter).
7. المفروض تشوف رسالة نجاح، ومفيش أخطاء حمرا.

هيتعمللك 5 جداول: `users`, `sessions`, `password_reset_requests`,
`search_rate_limit`, `admin_actions_log`.

> **⚠️ لو المشروع كان شغال عندك قبل كده، لازم تشغّل `schema.sql` تاني.**
> فيه جدول جديد اسمه `search_rate_limit` (بيمنع حد يقعد يجرّب أرقام واحد ورا
> التاني). الملف كله مكتوب بـ `CREATE TABLE IF NOT EXISTS`، فتشغيله تاني
> **مابيمسحش أي بيانات** ومابيكسّرش حاجة موجودة.
>
> لو نسيت الخطوة دي: البحث هيفضل شغال بس من غير حد للمحاولات، وهيطلع تحذير
> في لوجز Vercel فيه `search_rate_limit table missing`.

**عايز تتأكد إنها اتعملت؟** اكتب في نفس الـ SQL Editor:
```sql
SELECT table_name FROM information_schema.tables WHERE table_schema = 'public';
```
ودوس Run. المفروض تشوف أسامي الجداول الخمسة.

---

## الجزء 3: رفع الملفات على GitHub

1. روح على https://github.com واعمل **repository** جديد (لو معملتوش قبل كده) — سميه مثلاً `malg-app`. سيبه **Private** لحد ما تتأكد كل حاجة شغالة.
2. من جهازك، افتح مكان الفولدر `malg` في الترمينال (Command Prompt / Terminal)، واكتب الأوامر دي واحد واحد:

```bash
cd malg
git init
git add .
git commit -m "أول نسخة - تسجيل دخول وحساب"
git branch -M main
git remote add origin https://github.com/USERNAME/malg-app.git
git push -u origin main
```

(غيّر `USERNAME/malg-app` باسم حسابك واسم الـ repo الحقيقي اللي عملته).

**مهم جداً:** الملف `.gitignore` اللي عندك بيمنع رفع ملف `.env.local` (لو عملته) على GitHub — ده مقصود، عشان الأسرار متتسربش. متحاولش "تصلحه" أو تشيله.

---

## الجزء 4: ربط GitHub بـ Vercel

1. روح على https://vercel.com وسجل دخول (أو اعمل حساب لو معندكش).
2. دوس **Add New... → Project**.
3. اختار الـ repo `malg-app` اللي رفعته دلوقتي.
4. Vercel هيكتشف إنه مشروع Node.js تلقائي. سيب الإعدادات الافتراضية.
5. **قبل ما تدوس Deploy**، روح لقسم **Environment Variables** في نفس الصفحة، وضيف المتغيرات دي واحد واحد:

| الاسم (Name)              | القيمة (Value)                                    |
|----------------------------|----------------------------------------------------|
| `POSTGRES_URL`              | انسخها من صفحة Neon (نفس القيمة اللي حطيتها ***) |
| `JWT_SECRET`                | قيمة عشوائية طويلة (شوف الجزء 5 تحت)              |

6. دلوقتي دوس **Deploy**.
7. استنى دقيقة أو اتنين. لما يخلص، هيديك رابط زي `https://malg-app.vercel.app`.

**ملاحظة:** القيمة اللي في جدول `POSTGRES_URL` عندك أصلاً جاهزة — هي نفسها اللي ظهرتلك في صفحة Neon Quickstart (اللي بعتهالي بالنجوم ***). انسخها كوبي بيست زي ما هي بالظبط.

---

## الجزء 5: إزاي تعمل JWT_SECRET

ده مجرد كلمة سر طويلة عشوائية بيستخدمها السيرفر عشان "يوقّع" جلسات الدخول، مش لازم تفتكرها أو تكتبها في مكان تاني.

أسهل طريقة تولدها:
- روح على https://generate-secret.vercel.app/32 (موقع بسيط بيطلعلك قيمة عشوائية جاهزة)
- انسخ اللي هيطلعلك، وحطه كقيمة لـ `JWT_SECRET` في Vercel.

(أو لو عندك ترمينال: `openssl rand -hex 32`)

---

## الجزء 6: تجربة إن كل حاجة شغالة

1. افتح رابط الموقع اللي طلع من Vercel (`https://malg-app.vercel.app` أو اللي إنت سميته).
2. دوس تاب "حساب جديد"، اكتب اسم ورقم وباسورد، دوس "إنشاء حساب".
3. لو ظهرتلك شاشة "تسجيل الدخول تمام ✅" — يبقى كل حاجة شغالة: الموقع لقى السيرفر، السيرفر خزّن البيانات في Neon.
4. تأكيد إضافي: ارجع على Neon SQL Editor واكتب:
```sql
SELECT id, phone, name, created_at FROM users;
```
المفروض تلاقي الحساب اللي عملته دلوقتي ظاهر في النتيجة.

**لو ظهر خطأ؟** الغالب يكون:
- `POSTGRES_URL` متكتبتش صح في Vercel → ارجع الجزء 4.
- الجداول متعملتش في Neon → ارجع الجزء 2.
- بعد أي تعديل في Environment Variables في Vercel، لازم تعمل **Redeploy** من تبويب Deployments عشان القيم الجديدة تتفعّل.

---

## الجزء 7: تحويل الموقع لتطبيق أندرويد (APK)

بما إن الموقع بقى شكله PWA (فيه `manifest.json` و `sw.js`)، أسهل طريقة تعمله APK من غير ما تفتح أي برنامج برمجة زي Android Studio:

1. روح على https://www.pwabuilder.com
2. حط رابط موقعك (`https://malg-app.vercel.app`) في الخانة ودوس **Start**.
3. الموقع هيفحص مشروعك ويطلعلك تقييم (Manifest / Service Worker / Security) — المفروض الثلاثة يبقوا ✅ لأننا جهزناهم.
4. دوس **Package for stores** → اختار **Android**.
5. سيبله الإعدادات الافتراضية (أو غيّر اسم الباكدج لو عايز، مثلاً `com.malg.app`).
6. دوس **Generate** — هيديك ملف `.apk` (أو `.aab`) تقدر تنزله مباشرة وتجربه على موبايل أندرويد.

ده هيديك تطبيق أندرويد حقيقي بيفتح موقعك جوه إطار تطبيق (زي التطبيقات العادية)، من غير ما تكتب كود أندرويد خالص.

**ملاحظة مهمة:** الطريقة دي كويسة جداً للبداية والتجربة السريعة. لما نوصل لمرحلة المكالمات الفيديو (اللي محتاجة صلاحيات كاميرا/مايك/إشعارات بشكل أعمق)، ممكن نحتاج ننتقل لتطبيق أندرويد حقيقي (Kotlin) بدل الـ PWA — هنتكلم في ده لما نوصله.

---

## الجزء 8: تفعيل الرسائل الفورية (Firebase)

الرسائل قبل كده كانت بس شكل — بتتخزن في متصفحك إنت بس، عشان كده محدش كان بيستقبلها. دلوقتي الكود بقى بيبعت الرسائل فعلياً عن طريق **Firebase Realtime Database**، وبتوصل فوراً (زي واتساب بالظبط، من غير تحديث الصفحة).

### 8.1 اعمل مشروع Firebase
1. روح على https://console.firebase.google.com وسجل دخول بحساب Google.
2. دوس **Add project** واختار اسم (مثلاً `malg-app`)، وكمّل الخطوات الافتراضية.

### 8.2 فعّل الـ Realtime Database
1. من القائمة الجانبية: **Build → Realtime Database**.
2. دوس **Create Database**، اختار أقرب منطقة، وابدأ في وضع **Locked mode** (هنحط القواعد بنفسنا دلوقتي).
3. لما تتعمل، افتح تبويب **Rules** فوق، امسح اللي موجود، والصق بدله *كل* محتوى ملف `firebase-database-rules.json` اللي عندك في الفولدر، ودوس **Publish**.

> **⚠️ لو المشروع كان شغال عندك قبل كده، لازم تعيد لصق القواعد تاني.**
> القواعد اتغيرت تغيير جوهري — من غير التحديث ده، **الدردشة مش هتفتح خالص**
> والقائمة الرئيسية هتفضل فاضية. تفاصيل ليه في [الجزء 11](#الجزء-11-إيه-اللي-اتغير-وليه).

### 8.3 بيانات "تطبيق الويب" (عشان `firebase-client-config.js`)
1. من ⚙️ **Project settings** (بجانب Project Overview) → تحت **Your apps** → دوس أيقونة `</>` (Web).
2. اديله اسم واستكمل، مش محتاج Firebase Hosting.
3. هيديك object فيه `apiKey`, `authDomain`, `databaseURL`, `projectId`... انسخهم وحطهم مكان القيم في `public/firebase-client-config.js`.

> الـ `apiKey` بتاع الويب **مش سر** — ده معرّف عام مقصود إنه يبان في المتصفح.
> الحماية الحقيقية كلها في قواعد الـ Realtime Database اللي لصقتها فوق.
> بس مفتاح الـ **Admin SDK** (الجزء 8.4) ده سر حقيقي وماينفعش يتحط في الكود.

### 8.4 مفتاح Admin SDK (عشان السيرفر يقدر يصدّر توكنات)
1. برضو من ⚙️ **Project settings** → تبويب **Service accounts**.
2. دوس **Generate new private key** → هينزلك ملف JSON. **متشاركوش مع حد وميترفعش على GitHub خالص.**
3. من جوه الملف ده هتاخد 3 قيم وتحطهم Environment Variables في Vercel (زي `POSTGRES_URL` بالظبط، نفس المكان):

| الاسم في Vercel          | تجيبها منين من ملف الـ JSON                                   |
|---------------------------|----------------------------------------------------------------|
| `FIREBASE_PROJECT_ID`     | قيمة `project_id`                                              |
| `FIREBASE_CLIENT_EMAIL`   | قيمة `client_email`                                            |
| `FIREBASE_PRIVATE_KEY`    | قيمة `private_key` كاملة (انسخها زي ما هي، فيها `\n` جواها — الكود بيتعامل معاها تلقائي) |
| `FIREBASE_DATABASE_URL`   | من صفحة Realtime Database فوق، شكلها `https://malg-app-default-rtdb.<region>.firebasedatabase.app` |

4. بعد ما تضيفهم، لازم تعمل **Redeploy** من تبويب Deployments في Vercel عشان يتفعّلوا.

### 8.5 التجربة
1. سجل حساب على متصفحين مختلفين (أو متصفح عادي + نافذة incognito) بأرقام مختلفة.
2. من الحساب الأول، دور على رقم الحساب التاني وابعتله رسالة.
3. المفروض الرسالة تظهر فوراً عند الطرف التاني من غير ما يعمل Refresh للصفحة، وتظهر الدردشة في قائمته حتى لو هو معملش بحث عنك قبل كده.

**لو الرسايل مش بتوصل بعد الخطوات دي؟** الغالب يكون:
- **القواعد (Rules) متلصقتش صح في الجزء 8.2، أو نسيت تدوس Publish** ← ده أشهر
  سبب بفارق كبير. افتح Console (F12) ولو شفت `permission_denied` يبقى هي دي.
- قيم Environment Variables (خصوصاً `FIREBASE_PRIVATE_KEY`) متنسخش كاملة أو فيها مسافة زيادة.
- نسيت تعمل Redeploy بعد إضافة المتغيرات.
- الرسايل بتوصل على التلفون ومش بتوصل على الكمبيوتر (أو العكس)؟ ده **مش** فرق
  بين الأجهزة — ده كان بَق حقيقي في الترتيب اتصلّح، بس محتاج القواعد الجديدة.
  شوف [الجزء 11](#الجزء-11-إيه-اللي-اتغير-وليه).

---

## الجزء 8.6: إرسال الصور (Firebase — من غير أي خدمة خارجية)

دلوقتي تقدر تبعت صور في الشات (زرار الدبوس 📎 جنب صندوق الكتابة). بعد تجربة Firebase Storage (محتاج كارت بنكي) وCloudinary (تعقيد في الإعدادات)، استقرينا على أبسط وأوثق طريقة: **الصورة بتتضغط وتتصغّر جوه المتصفح نفسه، وبتتخزن كنص مباشرة جوه نفس الـ Firebase اللي الرسايل شغالة بيه** — من غير أي حساب أو خدمة خارجية تانية خالص.

**مفيش أي خطوة إضافية مطلوبة منك** — لو الرسايل شغالة عندك (الجزء 8)، الصور هتشتغل من غير أي إعداد زيادة.

### إزاي بيشتغل بالظبط

1. لما تختار صورة، المتصفح بيصغّرها (لحد 1280 بكسل أقصى عرض/طول) ويضغطها (JPEG) — تلقائيًا وبدون أي رفع لأي مكان
2. بعدين بيخزّنها كنص طويل (base64) مباشرة جوه رسالة الشات في Firebase، بالظبط زي أي رسالة نصية
3. الطرف التاني بيستقبلها زي أي رسالة تانية، ومتصفحه بيعرضها تلقائي

### حدود مهمة تعرفها

- الصورة الأصلية لحد 15 ميجا كملف، لكن بعد الضغط بتنزل لعشرات الكيلوبايت غالبًا (مثال حقيقي: صورة 2.6 ميجا نزلت لـ 90 كيلوبايت تقريبًا بعد الضغط)
- لو الصورة كبيرة جدًا وماعرفناش نضغطها كفاية، هتوصلك رسالة واضحة "الصورة كبيرة أوي حتى بعد الضغط"
- **الفيديو مش متاح حاليًا** — الطريقة دي (base64 جوه قاعدة بيانات) مناسبة للصور بس؛ الفيديوهات أكبر بكتير ومحتاجة حل تخزين منفصل (ممكن نضيفه بعدين لو احتجته فعلاً)
- **الرسايل بتتمسح من Firebase أول ما توصل للطرف التاني** (زي واتساب بالظبط): الصورة (كنص base64) بتتخزن في متصفح/تطبيق كل مستخدم على جهازه (`IndexedDB`) أول ما توصله، وبعدين بتتشال من Firebase على طول — فمتفضلش متراكمة على السيرفر ولا بتاخد من مساحة قاعدة البيانات المجانية على المدى الطويل
- **نتيجة طبيعية لده:** لو المستخدم فتح حسابه على جهاز جديد (تليفون تاني، أو مسح بيانات المتصفح)، مش هيلاقي تاريخ الشات القديم بما فيه الصور — لأنه كان متخزن على الجهاز القديم بس. ده نفس سلوك واتساب بالظبط (من غير نسخة احتياطية سحابية)

---

## الجزء 9: الأيقونات

أيقونات التطبيق موجودة جاهزة في `public/icons/`. لو عايز تغيّر الشكل أو
الألوان، عدّل `tools/make-icons.py` وشغّل:

```bash
npm run icons
```

السكربت Python خالص (بيكتب الـ PNG بإيده بـ zlib) — **مش** محتاج Pillow ولا
أي حزمة تتنزّل. لو `python` مش شغال عندك على ويندوز، جرّب `py` بدلها.

لو غيّرت الأيقونات، لازم تزوّد رقم النسخة في `public/sw.js` (السطر
`const VERSION = 'malg-v2'` → `malg-v3`) عشان المتصفحات تسيب النسخة القديمة
من الكاش.

---

## الجزء 10: الاختبارات

```bash
npm test
```

94 اختبار بيشتغلوا على `node:test` الجاهز في Node — **مفيش أي حزمة اختبار
محتاجة تتنزّل**، محتاج Node 18 أو أحدث بس. المفروض تشوف `pass 94` و`fail 0`.

أهم اللي بيتأكد منه:
- **الخصوصية:** كل رقم ناقص (`0`, `01`, `010`, ... `0101234567`) مرفوض، ومفيش
  `%` ولا `ILIKE` في أي استعلام.
- **id المحادثة:** نفس القيمة عند الطرفين، والشخص التالت مش طرف فيها.
- **القواعد:** الـ `.read` موجود على `userConversations/$uid` بالظبط.
- **مفيش نسختين** من نفس الكود يفرقوا عن بعض.
- **كل `id` بيستخدمه `app.js` موجود فعلًا في `index.html`**.

**بصراحة عن حدودها:** اختبارات `rules` و`api-privacy` و`wiring` بتقرا الملفات
وتتأكد إن الشروط مكتوبة صح — **مش** بتشغّل محرّك قواعد Firebase ولا بتضرب
داتابيز حقيقية. يعني هي بتمسك «حد شال القاعدة» بس مش بديل عن تجربة الجزء 8.5
على جهازين حقيقيين.

---

## الجزء 11: إيه اللي اتغير وليه

### الرسايل كانت بتظهر على التلفون ومش بتظهر على الكمبيوتر

ده كان **بَق حقيقي بيحصل لكل المستخدمين**، مش مشكلة في جهاز معيّن. سببه
حاجتين مع بعض:

1. **القواعد كانت بتطلب إن `participants` تكون مكتوبة الأول** عشان تسمح
   بقراءة الرسايل — بس الكتابة دي كانت بتحصل بعد ما الواجهة تبدأ تسمع. النتيجة
   `permission_denied` صامت، والشات بيقعد على «بيحمّل الرسايل...» للأبد.
   مين اللي كان بيشوف الرسايل؟ الجهاز اللي حصل إن الكتابة سبقت السماع فيه —
   وده بيختلف مع سرعة النت، فبيبان كأنه فرق بين تلفون وكمبيوتر.

   **الحل:** القواعد بقت بتشتق العضوية من الـ id بتاع المحادثة نفسه
   (`4_5` معناها المستخدمين 4 و5)، فمابقاش محتاج أي كتابة قبل القراءة. وكمان
   `app.js` بقى `await` على تسجيل المحادثة **قبل** ما يفتح أي سماعة.

2. **الفشل كان صامت.** كل `.on()` في Firebase بقى ليه معالج خطأ بيطلّع رسالة
   ظاهرة على الشاشة بدل ما يسكت.

### الدردشة كانت بتختفي من الشاشة الرئيسية بعد الخروج منها

كان فيه سببين:
- القواعد مكانش فيها `.read` على `userConversations/$uid` — والصلاحيات في
  Firebase بتنزل لتحت بس، فقاعدة على الابن مابتديش الأب حق القراءة.
- الدردشة كانت بتتسجّل عند **الإرسال** بس. دلوقتي بتتسجّل عند **فتح** الشات،
  فهي بتفضل في القائمة زي واتساب من غير ما تدوّر على الرقم تاني.

  (الطرف التاني لسه بيشوف الدردشة عند أول رسالة بس — ده مقصود، عشان محدش
  يشوف شاتات فاضية من ناس مايعرفهاش.)

### Firebase كانت بترفض حفظ القواعد: `No such method/property 'children'`

لو شفت الرسالة دي وإنت بتحاول تدوس Publish على القواعد:

```
Error saving rules – Line 7: No such method/property 'children'.
```

ده كان غلط حقيقي في ملف القواعد، **واتصلّح**. القصة: كنت مستخدم
`newData.children().length <= 2` عشان أمنع أي حد يضيف حقول زيادة مش متوقّعة.
بس `children()` **مش موجودة خالص** في محرّك قواعد Realtime Database — الحقل
`RuleDataSnapshot` مالوش أي طريقة تعدّ الأبناء. و Firebase لما تلاقي دالة مش
موجودة **بترفض الملف كله**، مش السطر بس، فالقواعد القديمة تفضل شغالة والجديدة
مابتنزلش.

الطريقة الرسمية لنفس الغرض هي حقل wildcard اسمه `$other` بيرفض أي مفتاح
مش معروف (المفاتيح المسمّاة بالاسم لها أولوية على الـ wildcard):

```json
"$other": { ".validate": false }
```

وده اللي موجود دلوقتي في تلات أماكن: `presence/$uid`،
`conversations/$convId/messages/$messageId`، و`userConversations/$uid/$otherUid`.
نفس الحماية بالظبط — أي حقل مش في القايمة بيترفض — بس بدالة موجودة فعلًا.

وعشان الغلط ده مايرجعش تاني، `tests/rules.test.js` فيه دلوقتي تلات اختبارات
جديدة: واحد بيمشي على **كل** تعبيرات القواعد ويرفض أي دالة مش في قايمة دوال
RTDB الحقيقية، وواحد بيمنع `children(` تحديدًا، وواحد بيتأكد إن `$other`
موجودة في التلات أماكن.

> **اعمل إيه دلوقتي:** ارجع لـ [الجزء 8.2](#82-فعّل-الـ-realtime-database)،
> انسخ محتوى `firebase-database-rules.json` **من الفولدر بعد التعديل** (مش
> النسخة القديمة اللي معاك)، والصقه في تبويب Rules ودوس **Publish**. المرة دي
> هيتقبل من غير أخطاء.

### البحث بقى بالرقم الكامل بس

قبل كده `010` كانت بترجّع كل حساب رقمه بيبدأ بـ `010` — ده تسريب بيانات.
دلوقتي أي رقم ناقص بيترفض قبل ما يوصل للداتابيز، والاستعلام مقارنة تساوي تام
بنتيجة واحدة، وفيه حد 20 بحثة كل 5 دقايق لكل مستخدم.

### تصميم جديد

دارك واحد بس (تباين عالي، زخرفة أقل). التلفون لوح واحد بيتبدّل بين القائمة
والشات مع زرار رجوع، والكمبيوتر (من 900px) لوحين جنب بعض زي واتساب ويب.
صندوق الكتابة بقى `<textarea>` بينزل تحت لغاية 168px بدل ما يفرّح الصف.
و«متصل الآن / آخر ظهور» بقت حقيقية (كانت نص ثابت)، ومعاها «بيكتب...» وعلامة
القراءة.

---

## إيه اللي بعد كده؟

1. **لوحة الأدمن** — تقدر تشوف كل المستخدمين، تحظر، توثق، تمسح حساب.
   (الأعمدة موجودة في `schema.sql` فعلًا، ناقص الواجهة والـ API.)
2. **استرجاع الباسورد** لسه ماشي على واتساب يدوي — محتاج OTP حقيقي.
3. **صور وملفات** في الرسايل.
4. **اختبارات على Firebase Emulator** بدل اختبارات شكل القواعد.

قولي نبدأ بإيه.
