{
  "name": "malg-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Malg — تطبيق دردشة (Backend API عن طريق Vercel Serverless Functions + Neon Postgres)",
  "scripts": {
    "test": "node --test \"tests/*.test.js\"",
    "icons": "python tools/make-icons.py"
  },
  "dependencies": {
    "@vercel/postgres": "^0.10.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "firebase-admin": "^12.6.0"
  }
}
